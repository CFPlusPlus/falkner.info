---
import Section from "../Section.astro";
import Divider from "../Divider.astro";
import ProjectSpotlight from "../ProjectSpotlight.astro";
import ProjectList from "../ProjectList.astro";

import type { Project } from "../../lib/site";

type Props = {
  spotlightProject: Project;
  projects: Project[];
  serverAddress: string;
};

const { spotlightProject, projects, serverAddress } = Astro.props;
---

<Section
  id="projects"
  title="Projekte"
  kicker="Dinge, an denen ich arbeite"
  lead="Hier sammle ich Projekte und Seiten, die für mich mehr sind als nur Links in einer Liste. Manche sind praktisch, manche eher persönlich gewachsen, und manche bekommen immer wieder neue kleine Verbesserungen, weil sie mir einfach wichtig sind.">
  <div class="space-y-10">
    <div class="reveal">
      <ProjectSpotlight project={spotlightProject} serverAddress={serverAddress} />
    </div>

    <div class="reveal" style="--reveal-delay: 80ms">
      <div class="meta">Mehr</div>
      <Divider class="mt-3" />
      <div class="mt-6">
        <ProjectList projects={projects} />
      </div>
    </div>
  </div>
</Section>
